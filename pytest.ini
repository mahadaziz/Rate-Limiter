[pytest]
testpaths = tests
# So `import app` resolves to the project root rather than the tests directory.
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -v --tb=short
